'use strict';

describe('lCApp collectionsList module', function() {

  var scope;
  var ctrl;
  beforeEach(inject(function($rootScope, $controller) {
    scope = $rootScope.$new();

    ctrl = $controller('CollectionsListCtrl', {
      $scope: scope
    });
  }));

  describe('collectionsList Controller', function(){


  });
});
